# Taste
- Communicates in Malay (Bahasa Malaysia) mixed with English technical terms and expects replies in the same language/register. Confidence: 0.8
- Works on a product split across separate repos/sessions (frontend e.g. `portfolio-astro`, backend e.g. `hono-workers`) and expects the agent to handle the related repo directly — read files, run commands, and edit code in it — instead of only being handed instructions or a diff. Confidence: 0.6
- Reports bugs by naming the failing user action plus the path/directory of the screen involved, and expects the agent to trace the flow end-to-end across frontend and backend APIs to find the real root-cause layer rather than only the layer named in the report. Confidence: 0.6
- After a fix, expects a proactive, comprehensive "check everything, what else is missing" audit of the whole integration surface (e.g. cross-checking every frontend API call against backend routes, method contracts, CSRF/CORS guards) rather than stopping at the reported bug; when findings are presented, actively asks for more ("point out lagi yang lain") and wants the audit kept going. Confidence: 0.75
- Wants work committed locally but never pushed/published without explicit approval — no push to main, no branch publication, no remote migrations or production data mutation unless explicitly requested. Confidence: 0.85
- Prefers staged, phase-by-phase implementation (small reviewable increments) over one large change, and will ask to "fix it step by step, phase by phase." Confidence: 0.8
- Treats production/remote data as off-limits during code work: wants idempotent, migration-safe SQL and code that works with the current schema; if a change cannot be safely applied, document it clearly instead of pretending it is done. Confidence: 0.85
- Expects detailed, explicit handoff/report artifacts (files under docs/reports, docs/plans) listing files changed, commands run and their results, what was done/skipped/blocked and why, and a suggested review checklist. Confidence: 0.8
- Wants strict, non-overclaiming status reporting (DONE / PARTIAL / BLOCKED / NOT DONE / INTENTIONALLY SKIPPED) and requires the agent to be explicit about anything only documented but not implemented. Confidence: 0.75
- Uses a second AI/tool (e.g. Claude) to review work afterward, so prioritizes clean structure, safe defaults, and easy review over speed. Confidence: 0.75
- Dislikes hardcoded data patches (e.g. hardcoded family member names in frontend merge logic) — prefers fixing root data issues and using stable identifiers over name-based matching. Confidence: 0.75
- Keeps changes scoped to the feature module and directly related docs; avoids unrelated redesigns, unnecessary dependency changes, and replacing working libraries unless proven impossible. Confidence: 0.8
- Prefers small pure, testable, framework-independent functions and removing duplicated logic (DRY). Confidence: 0.7
- Prefers server-side/SSR data fetching over client-side fetch waterfalls and cache-busting; wants existing backend/KV/CDN caching preserved. Confidence: 0.7
- Applies privacy-by-default minimization: restrict public payloads with strict whitelist-based DTO/sanitizers, strip notes/metadata/internal fields, and avoid exposing sensitive data (e.g. show year-only birth dates for living people). Confidence: 0.8
- Wants API clients to surface failures rather than mask them: reads may legitimately return null/empty on 404, but writes (POST/PUT/PATCH/DELETE) must throw so callers can never render a false "saved" success from a swallowed error. Confidence: 0.6
- Works through the audit findings incrementally by item number (e.g. "repair 1,2,3"), expecting only the selected items to be fixed in that round and the rest left for later — no blanket "fix everything at once". Confidence: 0.6
- Prefers security/privacy over convenience: will refuse to enable publicly-exposed or otherwise risky config (e.g. a public object-storage URL) just to make a feature work, defaults to private/least-privilege, and prefers removing or leaving a feature idle over shipping an insecure one. Confidence: 0.7
- Points the agent at another repo under `~/Developments` as the reference for established conventions/policies (e.g. storage security patterns) and expects it to read that codebase and mirror its patterns. Confidence: 0.6
- Prefers completing a half-built feature over deleting it: when told a UI element is "dead" because the backend is missing, will instead ask to build the missing backend (schema + service + route + dashboard wiring) so the existing frontend works, and expects the agent to restore/de-wire the code it removed rather than dropping the feature. Confidence: 0.6
